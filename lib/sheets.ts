/**
 * Punto de integración futuro con Google Sheets.
 * La V0 consume config/content.ts y no requiere credenciales ni red.
 */
export async function getContentFromSheets() {
  throw new Error("Integración con Google Sheets pendiente de implementación.");
}
